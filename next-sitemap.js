module.exports = {
  siteUrl: 'https://devlog.rweb.site',
  generateRobotsTxt: true, // (optional) Generate a robots.txt file
  sitemapSize: 7000,
  outDir: './public', // Ensures sitemap is in the 'public' folder
  filename: 'sitemap.xml', // This ensures the sitemap file is named 'sitemap.xml'
  changefreq: 'daily', // (optional) Set how often pages are likely to change
  priority: 0.7, // (optional) Set priority for pages
}
